# .github/workflows/security-check.yml
#
# Unified Terraform Security Check — runs on every PR touching terraform/
#
# Layers:
#   1. Checkov  — IaC static analysis (all terraform/ files)
#   2. tfsec    — deterministic rule scan (gold-tier modules only)
#   3. AI check — Azure OpenAI semantic analysis against MCSB Must-priority
#                 controls + exception gate (gold-tier modules only)
#
# Posts ONE unified comment on the PR. Updates it on re-runs.

name: "🔒 Terraform Security Check"

on:
  pull_request:
    branches: [develop]
    paths:
      - "terraform/**"

permissions:
  contents: read
  pull-requests: write

jobs:
  security:
    name: Security Scan
    runs-on: ubuntu-latest
    needs: checkov-tfplan
    if: always()

    steps:
      - name: Checkout
        uses: actions/checkout@v4
        with:
          fetch-depth: 0

      # ── Layer 1: Checkov ──────────────────────────────────────────────────
      - name: Install Checkov
        run: pip install checkov

      - name: Run Checkov
        id: checkov
        run: |
          checkov -d terraform/ \
            --framework terraform \
            --output cli \
            --output json \
            --output-file-path checkov-results/ \
            --soft-fail 2>&1 | tee checkov-output.txt
        continue-on-error: true

      - name: Extract Checkov summary
        id: checkov-summary
        run: |
          PASSED=$(grep -oP 'Passed checks: \K\d+' checkov-output.txt | tail -1 || echo "0")
          FAILED=$(grep -oP 'Failed checks: \K\d+' checkov-output.txt | tail -1 || echo "0")
          SKIPPED=$(grep -oP 'Skipped checks: \K\d+' checkov-output.txt | tail -1 || echo "0")
          echo "passed=${PASSED:-0}" >> $GITHUB_OUTPUT
          echo "failed=${FAILED:-0}" >> $GITHUB_OUTPUT
          echo "skipped=${SKIPPED:-0}" >> $GITHUB_OUTPUT

      # ── Detect gold-tier changes ──────────────────────────────────────────
      - name: Detect gold-tier Terraform changes
        id: changed
        run: |
          FILES=$(git diff --name-only origin/${{ github.base_ref }}...HEAD \
            -- 'terraform/modules/storage/**' \
               'terraform/modules/keyvault/**' \
               'terraform/modules/aks/**' \
            | tr '\n' ' ' | xargs)
          echo "files=${FILES}" >> $GITHUB_OUTPUT
          echo "Changed gold-tier files: '${FILES}'"

      # ── Layers 2 & 3: tfsec + AI (gold-tier only) ────────────────────────
      - name: Install tfsec
        if: steps.changed.outputs.files != ''
        run: |
          curl -s https://raw.githubusercontent.com/aquasecurity/tfsec/master/scripts/install_linux.sh \
            | bash

      - name: Run tfsec on gold-tier modules
        if: steps.changed.outputs.files != ''
        run: |
          tfsec terraform/modules/ \
            --include-passed \
            --format json \
            --out tfsec-output.json \
            --soft-fail
        continue-on-error: true

      - name: Set up Python
        if: steps.changed.outputs.files != ''
        uses: actions/setup-python@v5
        with:
          python-version: "3.11"

      - name: Install Python dependencies
        if: steps.changed.outputs.files != ''
        run: pip install requests

      # ── Download plan artifacts produced by the checkov-tfplan job ──────────
      - name: Download Terraform plan artifacts
        id: tf-plan
        if: steps.changed.outputs.files != ''
        uses: actions/download-artifact@v4
        with:
          name: terraform-plan-artifacts
        continue-on-error: true

      - name: Check plan availability
        id: plan-check
        if: steps.changed.outputs.files != ''
        run: |
          # Check that the file exists AND is non-empty (-s flag)
          # An empty file can be created by shell output redirect when terraform plan fails
          if [ -s terraform/environments/ci/tfplan.json ]; then
            echo "plan_ok=true" >> $GITHUB_OUTPUT
            echo "Terraform plan artifact available — plan-level context enabled"
          else
            echo "plan_ok=false" >> $GITHUB_OUTPUT
            echo "Terraform plan artifact not available or empty — AI analysis will use static scan only"
          fi

      - name: Run AI Security Check
        id: ai-check
        if: steps.changed.outputs.files != ''
        continue-on-error: true
        env:
          AZURE_API_KEY: ${{ secrets.AZURE_API_KEY }}
        run: |
          CHECKOV_ARG=""
          if [ -f checkov-results/results_json.json ]; then
            CHECKOV_ARG="--checkov-output checkov-results/results_json.json"
          fi
          CHECKOV_PLAN_ARG=""
          if [ -f checkov-plan-results/results_json.json ]; then
            CHECKOV_PLAN_ARG="--checkov-plan-output checkov-plan-results/results_json.json"
          fi
          PLAN_ARG=""
          if [ -f terraform/environments/ci/tfplan.json ]; then
            PLAN_ARG="--plan-file terraform/environments/ci/tfplan.json"
          fi
          python .github/scripts/azure_openai_tf_check.py \
            --changed-files ${{ steps.changed.outputs.files }} \
            --tfsec-output tfsec-output.json \
            --output ai-check-output.txt \
            $CHECKOV_ARG \
            $CHECKOV_PLAN_ARG \
            $PLAN_ARG

      - name: No gold-tier changes detected
        if: steps.changed.outputs.files == ''
        run: |
          printf '_No gold-tier Terraform changes detected — AI Security Check skipped._\n' \
            > ai-check-output.txt


      # ── Enforce gate decision ─────────────────────────────────────────────
      - name: Enforce security gate
        if: always()
        run: |
          if [ -f ai-check-output.txt ] && grep -q "Gate: BLOCKED" ai-check-output.txt; then
            echo "::error::Security gate BLOCKED — unregistered FAIL findings must be remediated or registered in exceptions-registry.json before merging."
            exit 1
          fi
          echo "Security gate passed."

      # ── Post unified report ───────────────────────────────────────────────
      - name: Post unified security report
        if: always()
        uses: actions/github-script@v7
        env:
          CHECKOV_PASSED:  ${{ steps.checkov-summary.outputs.passed }}
          CHECKOV_FAILED:  ${{ steps.checkov-summary.outputs.failed }}
          CHECKOV_SKIPPED: ${{ steps.checkov-summary.outputs.skipped }}
          TF_PLAN_OK:      ${{ steps.plan-check.outputs.plan_ok }}
        with:
          script: |
            const fs = require('fs');

            const passed  = parseInt(process.env.CHECKOV_PASSED  || '0');
            const failed  = parseInt(process.env.CHECKOV_FAILED  || '0');
            const skipped = parseInt(process.env.CHECKOV_SKIPPED || '0');

            let aiSection = '';
            try {
              aiSection = fs.readFileSync('ai-check-output.txt', 'utf8').trim();
            } catch (e) {
              aiSection = '_AI Security Check output not available._';
            }

            const blocked  = aiSection.includes('Gate: BLOCKED');
            const aiPassed = aiSection.includes('Gate: PASSED');
            const checkovOk = failed === 0;
            const planOk   = process.env.TF_PLAN_OK === 'true';

            const overall = blocked
              ? '❌ **Action required**'
              : (failed > 0 ? '⚠️ **Gate: PASSED** (Checkov warnings — see details)' : '✅ **All checks passed**');

            const checkovStatus = failed > 0 ? '⚠️ warning' : '✅ passed';
            const planWarning = !planOk
              ? `\n> ⚠️ **Terraform plan unavailable** — AI analysis based on static scan only (no plan context or plan-level Checkov)\n`
              : '';

            const now = new Date().toISOString().replace('T', ' ').substring(0, 19) + ' UTC';
            const runUrl = `${{ github.server_url }}/${{ github.repository }}/actions/runs/${{ github.run_id }}`;
            const run = `[#${{ github.run_number }}](${runUrl})`;

            const body = [
              `## 🔒 Terraform Security Report — ${overall}`,
              ``,
              `> PR **#${{ github.event.pull_request.number }}** · Branch \`${{ github.head_ref }}\` · Run ${run} · ${now}`,
              planWarning,
              `---`,
              ``,
              `### 📋 Checkov IaC Scan — ${checkovStatus}`,
              ``,
              `| Result | Count |`,
              `|---|---|`,
              `| ✅ Passed | ${passed} |`,
              `| ❌ Failed | ${failed} |`,
              `| ⏭️ Skipped (registered exceptions) | ${skipped} |`,
              ``,
              `---`,
              ``,
              aiSection,
            ].join('\n');

            const { data: comments } = await github.rest.issues.listComments({
              owner: context.repo.owner,
              repo: context.repo.repo,
              issue_number: context.issue.number,
            });

            const existing = comments.find(c =>
              c.user.type === 'Bot' && c.body.includes('🔒 Terraform Security Report')
            );

            if (existing) {
              await github.rest.issues.updateComment({
                owner: context.repo.owner,
                repo: context.repo.repo,
                comment_id: existing.id,
                body: body.slice(0, 65000),
              });
            } else {
              await github.rest.issues.createComment({
                owner: context.repo.owner,
                repo: context.repo.repo,
                issue_number: context.issue.number,
                body: body.slice(0, 65000),
              });
            }

  # ── Plan-level Checkov (fake ARM creds when real ones absent) ─────────────────
  checkov-tfplan:
    name: "Checkov (Terraform plan)"
    runs-on: ubuntu-latest
    if: always()
    continue-on-error: true

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Install Checkov
        run: pip install checkov

      - name: Log ARM credential availability
        run: |
          if [[ -n "$ARM_CLIENT_ID" ]]; then
            echo "Real ARM credentials detected — plan will authenticate with Azure."
          else
            echo "ARM credentials not configured — using placeholder creds with -refresh=false (no Azure API calls needed)."
          fi
        env:
          ARM_CLIENT_ID: ${{ secrets.ARM_CLIENT_ID }}

      # ── terraform plan → JSON → checkov ──────────────────────────────────────
      - name: Setup Terraform
        uses: hashicorp/setup-terraform@v3
        with:
          terraform_version: "~> 1.7"
          terraform_wrapper: false

      - name: Terraform Init (CI environment)
        working-directory: terraform/environments/ci
        run: terraform init -backend=false
        env:
          ARM_CLIENT_ID:       ${{ secrets.ARM_CLIENT_ID       || '00000000-0000-0000-0000-000000000000' }}
          ARM_CLIENT_SECRET:   ${{ secrets.ARM_CLIENT_SECRET   || 'fake-ci-secret-not-deployed' }}
          ARM_TENANT_ID:       ${{ secrets.ARM_TENANT_ID       || '00000000-0000-0000-0000-000000000000' }}
          ARM_SUBSCRIPTION_ID: ${{ secrets.ARM_SUBSCRIPTION_ID || '00000000-0000-0000-0000-000000000000' }}

      - name: Terraform Plan
        id: terraform-plan
        working-directory: terraform/environments/ci
        run: |
          terraform plan \
            -out=tfplan.out \
            -refresh=false \
            -input=false
        env:
          ARM_CLIENT_ID:       ${{ secrets.ARM_CLIENT_ID       || '00000000-0000-0000-0000-000000000000' }}
          ARM_CLIENT_SECRET:   ${{ secrets.ARM_CLIENT_SECRET   || 'fake-ci-secret-not-deployed' }}
          ARM_TENANT_ID:       ${{ secrets.ARM_TENANT_ID       || '00000000-0000-0000-0000-000000000000' }}
          ARM_SUBSCRIPTION_ID: ${{ secrets.ARM_SUBSCRIPTION_ID || '00000000-0000-0000-0000-000000000000' }}
        continue-on-error: true

      - name: Convert plan to JSON
        # Only run if the plan step succeeded — output redirect would otherwise create an
        # empty tfplan.json that confuses downstream jobs into treating plan as "available"
        if: steps.terraform-plan.outcome == 'success'
        working-directory: terraform/environments/ci
        run: terraform show -json tfplan.out > tfplan.json
        continue-on-error: true

      # ── Checkov: plan-level JSON (consumed by security job) ─────────────────
      - name: Checkov — plan-level JSON output
        run: |
          if [ -f terraform/environments/ci/tfplan.json ]; then
            echo "Running plan-level Checkov (JSON) for security job..."
            mkdir -p checkov-plan-results
            checkov \
              --file terraform/environments/ci/tfplan.json \
              --framework terraform_plan \
              --output json \
              --output-file-path checkov-plan-results/ \
              --soft-fail
          else
            echo "tfplan.json not available — skipping plan-level Checkov JSON"
          fi
        continue-on-error: true

      # ── Checkov: human-readable CLI output ───────────────────────────────────
      - name: Checkov — plan-level or static fallback (CLI)
        run: |
          if [ -f terraform/environments/ci/tfplan.json ]; then
            echo "Running plan-level Checkov analysis..."
            checkov \
              --file terraform/environments/ci/tfplan.json \
              --framework terraform_plan \
              --output cli \
              --soft-fail 2>&1 | tee checkov-plan-output.txt
          else
            echo "Plan unavailable — running focused static scan as fallback."
            echo "## Checkov static scan — gold-tier modules"
            for module in storage keyvault aks; do
              echo ""
              echo "### terraform/modules/${module}/"
              checkov -d terraform/modules/${module} \
                --framework terraform \
                --compact \
                --soft-fail 2>&1
            done | tee checkov-plan-output.txt
          fi
        continue-on-error: true

      - name: Upload Checkov CLI report
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: checkov-plan-report
          path: checkov-plan-output.txt
          if-no-files-found: ignore

      # ── Upload plan artifacts for consumption by the security job ────────────
      - name: Upload plan artifacts for security job
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: terraform-plan-artifacts
          path: |
            terraform/environments/ci/tfplan.json
            checkov-plan-results/
          if-no-files-found: ignore
          retention-days: 1
